# 🧠 Kavram Haritası (Concepts Map)

## Büyük Resim

Agentic AI'ın tüm kavramlarını ve birbirleriyle ilişkilerini burada bulabilirsiniz.

---

## 🏗️ Temel Mimari Kavramlar

### 1. Agent Execution Loop (Agent Çalışma Döngüsü)

Bir agent'ın kalbi **döngüdür**. Her agent şu adımları tekrarlar:

```
    ┌────────────────────────────────┐
    │         KULLANICI GÖREVİ       │
    │   "Hava durumunu öğren ve      │
    │    bana özet yaz"              │
    └───────────┬────────────────────┘
                │
                ▼
    ┌────────────────────────────────┐
    │   1. DÜŞÜN (Think/Reason)      │
    │   "Önce hava durumu aracını    │
    │    çağırmalıyım"               │
    └───────────┬────────────────────┘
                │
                ▼
    ┌────────────────────────────────┐
    │   2. KARAR VER (Plan/Decide)   │
    │   Aksiyon: get_weather tool    │
    │   Parametre: city="Istanbul"   │
    └───────────┬────────────────────┘
                │
                ▼
    ┌────────────────────────────────┐
    │   3. YÜRÜT (Act/Execute)       │
    │   Tool çağrılır, sonuç gelir:  │
    │   "İstanbul: 15°C, Güneşli"   │
    └───────────┬────────────────────┘
                │
                ▼
    ┌────────────────────────────────┐
    │   4. GÖZLEMLE (Observe)        │
    │   Sonuç yeterli mi?           │
    │   Evet → özet yaz, bitir      │
    │   Hayır → tekrar düşün        │
    └────────────────────────────────┘
```

**Modül:** Module 1

---

### 2. Task Decomposition (Görev Parçalama)

Büyük bir görevi küçük, yönetilebilir adımlara bölme:

```
BÜYÜK GÖREV: "Bir blog yazısı yaz ve yayınla"
    │
    ├── Adım 1: Konu araştırması yap
    ├── Adım 2: Taslak oluştur
    ├── Adım 3: Taslağı gözden geçir
    ├── Adım 4: Düzeltmeleri uygula
    └── Adım 5: Yayınla
```

**Modül:** Module 1, Module 5

---

### 3. Degrees of Autonomy (Otonomi Seviyeleri)

Agent ne kadar bağımsız çalışmalı?

```
DÜŞÜK OTONOMİ ◄──────────────────────► YÜKSEK OTONOMİ

Human-in-the-Loop     Human-on-the-Loop     Fully Autonomous
(HITL)                (HOTL)                 (Tam Otonom)
│                     │                      │
│ Her adımda          │ Agent çalışır,       │ Agent tamamen
│ insan onayı         │ insan izler,         │ bağımsız çalışır
│ gerekir             │ gerektiğinde müdahale │
│                     │ eder                  │
│ Örnek:              │ Örnek:               │ Örnek:
│ "Bu e-postayı       │ "Araştırma yap,      │ "Sunucu
│  göndereyim mi?"    │  ben kontrol          │  monitörleme
│                     │  edeceğim"            │  ve otomatik
│                     │                       │  düzeltme"
```

**Modül:** Module 1

---

## 🪞 Reflection (Yansıtma) Kavramları

### 4. Reflection Pattern

Agent'ın kendi çıktısını değerlendirmesi:

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  ÜRET    │ ──► │ ELEŞTİR  │ ──► │ GELİŞTİR │
│ (Generate)│     │ (Critique)│     │ (Improve) │
└──────────┘     └──────────┘     └──────────┘
     ▲                                  │
     └──────────────────────────────────┘
              Döngü (gerekirse)
```

**Modül:** Module 2

---

### 5. External Feedback (Dış Geri Bildirim)

```
Agent Çıktısı ──► Doğrulama Aracı ──► Sonuç
                  (MCP validate)
                       │
                ┌──────┴──────┐
                │             │
            ✅ Geçerli    ❌ Hatalı
            (devam et)    (düzelt ve
                          tekrar dene)
```

**Modül:** Module 2

---

## 🛠️ Tool Use & MCP Kavramları

### 6. MCP Mimarisi

```
┌─────────────────────────────────────────────────────┐
│                    MCP EKOSİSTEMİ                    │
│                                                       │
│  ┌─────────┐   JSON-RPC    ┌─────────────────────┐  │
│  │  Agent   │◄────────────►│    MCP Server        │  │
│  │ (Client) │   mesajlar    │                     │  │
│  └─────────┘               │  ┌───────────────┐  │  │
│                             │  │ Tool Registry │  │  │
│                             │  └───────┬───────┘  │  │
│                             │          │          │  │
│                             │  ┌───┬───┼───┬───┐ │  │
│                             │  │   │   │   │   │ │  │
│                             │  │ T1│ T2│ T3│ T4│ │  │
│                             │  └───┴───┴───┴───┘ │  │
│                             └─────────────────────┘  │
│                                                       │
│  T1: echo    T2: time    T3: search   T4: file_write │
└─────────────────────────────────────────────────────┘
```

**Modül:** Module 1 (giriş), Module 3 (derinlemesine)

---

### 7. Tool Schema & Contracts

Her tool'un bir "sözleşmesi" vardır:

```json
{
  "name": "get_weather",
  "description": "Belirtilen şehrin hava durumunu getirir",
  "parameters": {
    "type": "object",
    "properties": {
      "city": {
        "type": "string",
        "description": "Şehir adı (örn: Istanbul)"
      }
    },
    "required": ["city"]
  }
}
```

Bu şema:
- Agent'a tool'un ne yaptığını anlatır
- Parametreleri doğrular
- Hatalı çağrıları önler

**Modül:** Module 3

---

### 8. Error Handling Patterns (Hata Yönetimi)

```
Tool Çağrısı
     │
     ├── ✅ Başarılı → Sonucu döndür
     │
     ├── ⏱️ Timeout → Retry (tekrar dene)
     │       └── Max retry aşıldı → Hata bildir
     │
     ├── ❌ Geçersiz parametre → Hata mesajı döndür
     │
     └── 💥 Beklenmedik hata → Log'la + fallback
```

**Modül:** Module 3

---

## 📊 Eval & Optimization Kavramları

### 9. Evaluation Seviyeleri

```
┌─────────────────────────────────────────────┐
│           EVALUATION PİRAMİDİ               │
│                                             │
│              ┌─────────┐                    │
│              │  E2E    │  ← Görev başarılı  │
│              │  Eval   │    mı? (en üst)    │
│              └────┬────┘                    │
│                   │                         │
│          ┌────────┴────────┐                │
│          │  Component      │  ← Her parça   │
│          │  Eval           │    doğru mu?    │
│          └────────┬────────┘                │
│                   │                         │
│     ┌─────────────┴─────────────┐           │
│     │  Unit Eval                │  ← Tek    │
│     │  (tool, prompt, vb.)      │    birim   │
│     └───────────────────────────┘           │
└─────────────────────────────────────────────┘
```

**Modül:** Module 4

---

### 10. Cost Optimization (Maliyet Optimizasyonu)

```
SORUN: Her LLM çağrısı para!

STRATEJİLER:
┌────────────────────────────┐
│ 1. Model Routing           │
│    Basit görev → ucuz model│
│    Zor görev → pahalı model│
├────────────────────────────┤
│ 2. Context Compression     │
│    Gereksiz mesajları kırp │
├────────────────────────────┤
│ 3. Caching                 │
│    Aynı soruyu tekrar      │
│    sorma, cache'den al     │
├────────────────────────────┤
│ 4. Fewer Calls             │
│    Döngü sayısını azalt    │
└────────────────────────────┘
```

**Modül:** Module 4

---

## 🤖 Multi-Agent Kavramları

### 11. Agent Rolleri

```
┌─────────────────────────────────────────────────┐
│              ARAŞTIRMA TAKIMI                     │
│                                                   │
│  ┌──────────┐    görev dağıt    ┌──────────┐    │
│  │ PLANNER  │ ───────────────── │RESEARCHER│    │
│  │ (Planlayıcı)                 │(Araştırmacı)  │
│  └──────────┘                   └──────────┘    │
│       │                              │           │
│       │ koordine et          bulgular│           │
│       │                              │           │
│  ┌──────────┐    geri bildirim ┌──────────┐    │
│  │SYNTHESIZER│◄────────────── │  CRITIC   │    │
│  │(Sentezleyici)               │(Eleştirmen)│   │
│  └──────────┘                  └──────────┘    │
└─────────────────────────────────────────────────┘
```

**Modül:** Module 5

---

### 12. Shared Memory (Paylaşılan Hafıza)

```
┌──────────┐   yaz    ┌─────────────┐   oku    ┌──────────┐
│ Agent A  │ ───────► │  SHARED     │ ◄─────── │ Agent B  │
│          │          │  MEMORY     │          │          │
└──────────┘          │  (MCP Tool) │          └──────────┘
                      │             │
                      │ key: value  │
                      │ key: value  │
                      └─────────────┘
                           ▲
                           │ oku/yaz
                      ┌──────────┐
                      │ Agent C  │
                      └──────────┘
```

**Modül:** Module 5

---

## 🔗 Kavramlar Arası İlişki Haritası

```
Agent Loop ◄──── Task Decomposition
    │
    ├──► Tool Use (MCP) ◄──── Tool Schema
    │         │
    │         ├──► Error Handling
    │         │
    │         └──► Tool Registry
    │
    ├──► Reflection ◄──── External Feedback
    │
    ├──► Evaluation
    │         │
    │         ├──► Cost Optimization
    │         │
    │         └──► Model Routing
    │
    └──► Multi-Agent
              │
              ├──► Agent Roles
              │
              ├──► Shared Memory
              │
              └──► Orchestration
```

---

> 💡 **İpucu:** Her kavramı ilgili modülde pratikte göreceksiniz. Bu harita, büyük resmi anlamanız içindir.
